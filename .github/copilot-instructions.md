# Repository instructions

Follow `/AGENTS.md` and the nearest nested `AGENTS.md`. Use root npm scripts, preserve static export and base paths, keep secrets out of source, and run `npm run check` before proposing completion.
