# Execution plans

Use `template.md` for changes that span applications, data migrations or deployment behavior. Keep active plans here while work is in progress, then move the final record to `docs/archive/tasks/` or document the durable decision as an ADR.
