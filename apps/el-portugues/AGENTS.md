# El Portugués application

- Preserve the `/elportugues-site` base path, trailing slashes and static export.
- Keep fallback JSON content usable when Supabase is unavailable.
- Browser configuration belongs in `.env.local` and must be represented with placeholders in `.env.example`.
- Validate with `npm run build:elportugues` from the repository root.
