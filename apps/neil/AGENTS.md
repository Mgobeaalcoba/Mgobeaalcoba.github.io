# Neil application

- Preserve the `/neil-site` base path, trailing slashes and static export.
- User-facing content is multilingual; update every supported locale when changing copy.
- Browser configuration belongs in `.env.local` and must be represented with placeholders in `.env.example`.
- Validate with `npm run build:neil` from the repository root.
